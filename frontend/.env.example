# The base URL of the deployed backend API
# If deployed on Railway, this should be the public URL of the backend service (e.g., https://backend-production-xyz.up.railway.app/api)
VITE_API_URL=http://localhost:5000/api
